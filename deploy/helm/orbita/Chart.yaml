apiVersion: v2
name: orbita
description: A strongly consistent, multitenant key-value store.
type: application

# The chart version tracks the chart, and appVersion tracks the binary. They
# move independently because a fix to a template is not a release of the store.
version: 0.1.0
appVersion: "0.0.1"

home: https://github.com/orbita-rocks/orbita
sources:
  - https://github.com/orbita-rocks/orbita
keywords:
  - key-value
  - coordination
  - consistency
