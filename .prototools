# Pins the version of moon itself. moon pins the Rust toolchain, and this pins
# moon, so `proto use` in a fresh checkout produces the same build environment
# CI runs. Rust is deliberately not listed here: it belongs to
# .moon/toolchain.yml, which is the file that also writes rust-toolchain.toml.
moon = "1.41.7"
